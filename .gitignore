# === Secrets (NEVER commit) ===
.env
.env.*
!.env.example
*.env
*.env.backup
*.bak
*.key
*.pem
secrets/
deploy/.env
deploy/.env.backup

# === Internal memory file (contains real secrets, local-only, NEVER push) ===
memory.md

# === Credentials quick-reference (local-only, NEVER push) ===
credentials.md

# === Customer-owned reference docs (Project 1 — moved to proposal-estimation/) ===
proposal-estimation/docs/apidocs_spa_proposal_estimation_update.pdf
proposal-estimation/docs/pages/
proposal-estimation/docs/apidocs_extracted.txt

# === OS / Editor ===
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*~

# === Node / build artifacts (just in case) ===
node_modules/
dist/
build/
*.log
